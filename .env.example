# Server port (optional, defaults to 3171)
# PORT=3171

# Data directory for wiki pages (optional, defaults to '.delve/data')
# DATA_DIR=.delve/data

# HTTPS/SSL configuration (optional)
# To enable HTTPS with locally-trusted certificates:
# 1. Install mkcert: brew install mkcert (macOS) or see https://github.com/FiloSottile/mkcert
# 2. Install local CA: mkcert -install
# 3. Generate certs: mkcert -cert-file certs/localhost.pem -key-file certs/localhost-key.pem localhost 127.0.0.1
# 4. Enable SSL below:
# SSL_ENABLED=true
# SSL_CERT=certs/localhost.pem
# SSL_KEY=certs/localhost-key.pem
